# Suppose a date is represented as a tuple (d, m, y).Create two date tuples &
# find the number of days between the two dates.

print("Enter the dates in the (d,m,y) format.")
date_1=tuple(input(f"Date_1:"))
date_2=tuple(input(f"Date 2:"))

print(date_1)